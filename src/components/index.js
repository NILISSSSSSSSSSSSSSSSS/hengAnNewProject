import './loading'
import Vue from 'vue'
import passwordInput from './passwordInput'
import backLayout from './backLayout'
import card from './card'
import button from './bigButton'
import codeInput from './getCodeInput'

Vue.component(passwordInput.name, passwordInput)
Vue.component(backLayout.name, backLayout)
Vue.component(card.name, card)
Vue.component(button.name, button)
Vue.component(codeInput.name, codeInput)

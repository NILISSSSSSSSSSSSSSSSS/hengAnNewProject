import {debounce} from './debounce'
import {setCookie, getCookie, delCookie} from './cookie'
import {setItem, getItem, clearItem} from './storage'
import { pwVerify, phoneVerify, emailVerify } from './verify'

export default {
  debounce,
  setCookie,
  getCookie,
  delCookie,
  setItem,
  getItem,
  clearItem,
  pwVerify,
  phoneVerify,
  emailVerify
}
